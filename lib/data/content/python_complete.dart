import '../../shared/models/content_models.dart';
import 'python_complete_more.dart';
import 'python_complete_rest.dart';
import 'python_gap.dart';
import 'syllabus_foundations.dart';

const _t = 'track_python';

Lesson pyTopic(
  String id,
  String course,
  String title,
  String sub,
  String what,
  String why,
  String how,
  String quiz,
  String ok,
  String c1,
  String e1,
  String c2,
  String e2, {
  List<String> wrong = const ['Unrelated', 'The opposite', 'Not how Python works'],
}) {
  return u(
    id,
    _t,
    course,
    title,
    sub,
    what,
    why,
    how,
    quiz,
    ok,
    wrong: wrong,
    code: c1,
    expected: e1,
    code2: c2,
    expected2: e2,
  );
}

/// Remaining Python Programming topics as dedicated courses.
Map<String, List<Lesson>> pythonCompleteCourses() => {
      'course_py_fundamentals': fundamentalsTopics(),
      'course_py_types_deep': typesDeepTopics(),
      'course_py_strings_deep': stringsDeepTopics(),
      ...pythonCompleteMore(),
      ...pythonCompleteRest(),
      ...pythonGapCourses(),
    };

List<Lesson> fundamentalsTopics() {
  const c = 'course_py_fundamentals';
  return [
    pyTopic('py_install_01', c, 'Installing Python', 'Get a real interpreter on your machine',
        'Install CPython from python.org or your OS package manager. Check with `python3 --version`. The app sandbox is a subset; a local install is the real language.',
        'You cannot ship packages, venvs, or notebooks without an interpreter on disk.',
        'Install 3.12+. Confirm `python3` on PATH. Never train on an unnamed “python” that might be 2.7.',
        'You confirm a working install with', 'python3 --version',
        'print("3.12.0")\nprint("ok")', '3.12.0\nok',
        'v = "3.12"\nprint(v)\nprint("installed")', '3.12\ninstalled',
        wrong: ['pip only, no interpreter', 'A browser bookmark', 'A GPU driver']),
    pyTopic('py_interp_01', c, 'The Python Interpreter', 'REPL vs a file — two ways to run code',
        'The interpreter reads source, compiles bytecode, and runs it on the PVM. A REPL is one statement at a time. A file is a program.',
        'Interactive experiments lie if you never rerun them as a script.',
        'Use the REPL to poke. Save anything you will reuse as a `.py` file.',
        'A REPL is best for', 'Short experiments you will throw away or rewrite as a file',
        'print("repl")\nprint(1 + 1)', 'repl\n2',
        'x = 2\nprint(x * 3)', '6',
        wrong: ['Training a model overnight', 'Shipping an API', 'Editing bytecode by hand']),
    pyTopic('py_run_01', c, 'Running Python Programs', 'A file is a program; the interpreter is the runner',
        '`python3 app.py` runs a file top to bottom. The first argument after the file is `sys.argv[1]`. Exit code 0 means success.',
        'A notebook cell is not a job. Training belongs in a file you can rerun.',
        'Put the entrypoint in `if __name__ == "__main__"` once you learn modules.',
        'python3 train.py runs', 'The file from top to bottom in a new process',
        'print("running")\nprint("done")', 'running\ndone',
        'step = 1\nprint(f"step {step}")', 'step 1',
        wrong: ['Only the last function', 'Only comments', 'The GPU firmware']),
    pyTopic('py_comments_01', c, 'Comments', 'Notes for humans — the machine skips them',
        'A `#` starts a comment to the end of the line. Comments explain why, not what the next token already says.',
        'A lying comment is worse than none. Stale comments ship bugs.',
        'Comment the invariant, the unit, or the surprising constraint.',
        'Python comments begin with', '#',
        'print(1)\n# skipped\nprint(2)', '1\n2',
        '# setup\nx = 3  # batch size\nprint(x)', '3',
        wrong: ['//', '<!-- -->', '/* */ only']),
    pyTopic('py_const_01', c, 'Constants', 'A name you promise not to rebind',
        'Python has no true constants. By convention, `UPPER_SNAKE` means “do not reassign.” The object can still be mutable.',
        'A “constant” list you append to is a shared trap.',
        'Use uppercase for config scalars. Prefer tuples or frozen dataclasses for records.',
        'MAX_BATCH = 32 is a', 'Convention — the name should not be rebound',
        'MAX_BATCH = 32\nprint(MAX_BATCH)\nprint(MAX_BATCH * 2)', '32\n64',
        'PI = 3.14\nprint(PI)', '3.14',
        wrong: ['A compiler-enforced immutable', 'A C #define', 'A GPU register']),
    pyTopic('py_naming_01', c, 'Naming Conventions', 'PEP 8 names are a readability contract',
        'Modules and functions: `snake_case`. Classes: `CapWords`. Constants: `UPPER_SNAKE`. Leading `_` means internal.',
        'A name is documentation. `x2` in a trainer is a bug waiting for a reviewer.',
        'Name the thing, not the type. `learning_rate` not `float1`.',
        'A class name in PEP 8 is written in', 'CapWords (PascalCase)',
        'learning_rate = 0.001\nprint(learning_rate)', '0.001',
        'class Model:\n    pass\nprint("Model")', 'Model',
        wrong: ['ALLCAPS always', 'kebab-case', 'Spaces']),
    pyTopic('py_print_01', c, 'print()', 'Write values to stdout — the first debugger',
        '`print(*objects, sep=" ", end="\\n")` converts each object with `str` and writes them. It is I/O, not a return value.',
        'If you cannot print a shape, you cannot debug a tensor.',
        'Print one fact per line when you are hunting a bug. Do not print secrets.',
        'print(1, 2) writes', '1 2 followed by a newline',
        'print("hi")\nprint(1, 2)\nprint(True)', 'hi\n1 2\nTrue',
        'print("a", "b")\nprint(3 + 4)', 'a b\n7',
        wrong: ['Returns a string', 'Writes to a file named print', 'Only works in notebooks']),
    pyTopic('py_input_01', c, 'input()', 'Read one line from the user as a string',
        '`input(prompt)` always returns a `str`. Convert with `int()`/`float()` at the boundary. This sandbox answers `Ada`.',
        'Comparing `input()` to an int without converting is a silent false.',
        'Parse immediately. Never keep raw strings as numbers.',
        'input() always returns a', 'str',
        'name = input("name: ")\nprint(name)\nprint(type(name))', "Ada\n<class 'str'>",
        'name = input()\nprint(f"hi {name}")', 'hi Ada',
        wrong: ['int', 'bytes', 'None']),
    pyTopic('py_op_arith_01', c, 'Arithmetic Operators', '+ - * / // % **',
        '`/` always floats. `//` floors toward -∞. `%` is remainder. `**` is power.',
        'Batch math and learning-rate schedules are these operators with names.',
        'Use `//` for counts. Use `/` for rates. Watch float noise.',
        '7 // 2 equals', '3',
        'print(7 / 2)\nprint(7 // 2)\nprint(7 % 2)\nprint(2 ** 3)', '3.5\n3\n1\n8',
        'print(10 // 3)\nprint(10 % 3)', '3\n1',
        wrong: ['3.5', '4', '2']),
    pyTopic('py_op_assign_01', c, 'Assignment Operators', 'A name is rebound — += is read, then write',
        '`=` binds a name. `x = x + 1` reads the old value, then rebinds. Augmented forms (`+=`) do the same.',
        'On a list, `+=` mutates in place. On an int, it rebinds. That split causes alias bugs.',
        'For numbers, write `x = x + n`. For lists you mean to grow, `append` is clearer.',
        'x = x + 1 does', 'Read x, add 1, rebind the name x',
        'x = 3\nx = x + 2\nprint(x)\nx = x * 2\nprint(x)', '5\n10',
        'n = 1\nn = n + n\nprint(n)', '2',
        wrong: ['Create a new language', 'Freeze x', 'Copy every object in memory']),
    pyTopic('py_op_cmp_01', c, 'Comparison Operators', '== != < > <= >= — they return bool',
        'Comparisons produce `True` or `False`. Chain only when you mean a range (`1 < x < 5`). Prefer explicit checks in model code.',
        'A float `==` on a loss is usually a lie.',
        'Compare ints and lengths. Round floats before equality.',
        '3 < 4 evaluates to', 'True',
        'print(3 < 4)\nprint(3 == 3)\nprint(3 != 4)\nprint(5 >= 5)', 'True\nTrue\nTrue\nTrue',
        'print(2 <= 1)\nprint(2 > 2)', 'False\nFalse',
        wrong: ['4', 'None', '3']),
    pyTopic('py_op_logic_01', c, 'Logical Operators', 'and, or, not — short-circuit',
        '`and`/`or` return an operand, not always a bool. `not` always returns a bool. Evaluation stops early (short-circuit).',
        '`if x and x.flag` is safe because `x.flag` is skipped when `x` is falsey.',
        'Write `if ready and valid:` not nested flags you cannot name.',
        'not True is', 'False',
        'print(True and False)\nprint(True or False)\nprint(not False)', 'False\nTrue\nTrue',
        'ready = True\nvalid = False\nprint(ready and valid)\nprint(ready or valid)', 'False\nTrue',
        wrong: ['True', 'None', '0']),
    pyTopic('py_op_bit_01', c, 'Bitwise Operators', '& | ^ on integers — flags and masks',
        '`&` and, `|` or, `^` xor, applied bit by bit on ints. Useful for flags, not for floats.',
        'Token IDs and packed feature flags are ints. A wrong mask is a silent wrong feature.',
        'Prefer named flags over magic hex until you need the bits.',
        '5 & 3 equals', '1',
        'print(5 & 3)\nprint(5 | 3)\nprint(5 ^ 3)', '1\n7\n6',
        'print(1 & 1)\nprint(1 | 2)', '1\n3',
        wrong: ['8', '0', '15']),
    pyTopic('py_op_mem_01', c, 'Membership Operators', 'in / not in — does the collection contain this?',
        '`x in seq` is True if the item (or substring, or key) is present. On dicts, `in` tests keys.',
        '`if label in allowed` is the cheapest validation you will write.',
        'Use `in` on a set for many lookups. Use `in` on a string for substrings.',
        '"a" in "cat" is', 'True',
        'print("a" in "cat")\nprint(1 in [1, 2])\nprint(3 not in [1, 2])\nprint("k" in {"k": 1})', 'True\nTrue\nTrue\nTrue',
        'print("z" in "cat")\nprint(0 in {0, 1})', 'False\nTrue',
        wrong: ['False', 'None', 'Error']),
    pyTopic('py_op_id_01', c, 'Identity Operators', 'is / is not — same object, not same value',
        '`is` tests object identity. `==` tests value. Use `is` for `None` (and sometimes True/False). Never use `is` for strings you built.',
        '`x is None` is the None check. `x == None` works but is the wrong habit.',
        'Alias two names to one list, then compare `is` vs `==`.',
        'x is None should be used when', 'You want to know if the name is the None object',
        'a = [1]\nb = a\nc = [1]\nprint(a is b)\nprint(a is c)\nprint(a == c)', 'True\nFalse\nTrue',
        'x = None\nprint(x is None)\nprint(x is not None)', 'True\nFalse',
        wrong: ['You want numeric equality', 'You compare two file paths', 'You sort a list']),
    pyTopic('py_op_prec_01', c, 'Operator Precedence', '** then * / then + then comparisons then and/or',
        'Python evaluates `**` first, then multiply/divide, then add, then comparisons, then `not`/`and`/`or`. Parentheses win arguments.',
        'A missing parenthesis in a loss is a wrong model, not a syntax error.',
        'When in doubt, parenthesize. Do not memorize the whole table.',
        '2 + 3 * 4 equals', '14',
        'print(2 + 3 * 4)\nprint((2 + 3) * 4)\nprint(2 ** 3 * 2)', '14\n20\n16',
        'print(1 + 2 * 3 + 4)\nprint(10 - 2 * 3)', '11\n4',
        wrong: ['20', '24', '9']),
  ];
}

List<Lesson> typesDeepTopics() {
  const c = 'course_py_types_deep';
  return [
    pyTopic('py_complex_01', c, 'Complex Numbers', 'real + imag j — two floats with a rule',
        'A complex number is a pair (real, imag). Python writes `1+2j`. This lesson stores the pair as a tuple you can add component-wise.',
        'FFT and some signal models live here. Most ML tensors stay real.',
        'Add component-wise. Do not pretend a pair is a scalar.',
        'A complex number is best thought of as', 'A real part and an imaginary part',
        'z = (1, 2)\nw = (3, 4)\nprint(z[0] + w[0], z[1] + w[1])', '4 6',
        'real, imag = 0, 1\nprint(real, imag)', '0 1',
        wrong: ['A string', 'A bool', 'A file path']),
    pyTopic('py_mut_01', c, 'Mutable vs Immutable', 'Can this object change in place?',
        'Ints, floats, bools, strings, tuples (of immutables) are immutable. Lists, dicts, sets are mutable. Names are not boxes — they point.',
        'A default list argument mutates across calls because the list is one object.',
        'If two names point at a list, `append` is visible through both.',
        'Which type is immutable?', 'str',
        's = "ab"\nt = s + "c"\nprint(s)\nprint(t)', 'ab\nabc',
        'xs = [1]\nys = xs\nys.append(2)\nprint(xs)', '[1, 2]',
        wrong: ['list', 'dict', 'set']),
    pyTopic('py_cast_01', c, 'Type Conversion', 'int / float / str / bool at the boundary',
        '`int("3")`, `float("1.5")`, `str(3)`, `bool([])` convert. Conversion is explicit. Garbage in raises.',
        'CSV columns arrive as strings. Models need numbers. The cast is a product decision.',
        'Convert once at the I/O edge. Keep the rest typed.',
        'int("12") + 1 equals', '13',
        'print(int("12") + 1)\nprint(float("1.5") + 1)\nprint(str(3) + "x")\nprint(bool(""))', '13\n2.5\n3x\nFalse',
        'print(int(3.9))\nprint(str(True))', '3\nTrue',
        wrong: ['"121"', '12.1', 'None']),
    pyTopic('py_typefn_01', c, 'type()', 'Ask an object what class it is',
        '`type(x)` returns the class. In this sandbox it prints like `<class \'int\'>`. Use it to debug, not to branch a design.',
        'A surprise type is usually a leaky cast or a None you did not handle.',
        'Print `type(x)` next to `x` when a function got the wrong thing.',
        'type(True) reports', 'bool',
        'print(type(3))\nprint(type(1.5))\nprint(type(True))\nprint(type("ai"))', "<class 'int'>\n<class 'float'>\n<class 'bool'>\n<class 'str'>",
        'print(type([1]))\nprint(type({"k": 1}))', "<class 'list'>\n<class 'dict'>",
        wrong: ['int', 'str', 'None']),
    pyTopic('py_isinstance_01', c, 'isinstance()', 'Is this object this type (or a subclass)?',
        '`isinstance(x, int)` is True for ints. Prefer it over `type(x) is int` so subclasses count. Check unions by calling twice.',
        'A trainer that only accepts `list` will reject a custom sequence you wanted.',
        'Validate public APIs with isinstance. Do not type-check every local.',
        'isinstance(3, int) is', 'True',
        'print(isinstance(3, int))\nprint(isinstance("a", str))\nprint(isinstance(3, str))\nprint(isinstance([1], list))', 'True\nTrue\nFalse\nTrue',
        'print(isinstance(1.5, float))\nprint(isinstance(True, bool))', 'True\nTrue',
        wrong: ['False', '3', 'int']),
    pyTopic('py_none_type_01', c, 'Boolean, None & Empty Values', 'False, None, and empty are different ideas',
        '`False` is a bool. `None` is the missing object. `[]`/`""`/`0` are falsey but not None. Test the one you mean.',
        '`if not xs` treats empty and None the same. Sometimes that hides a bug.',
        'Use `is None` for missing. Use `len` for empty. Use `== False` almost never.',
        'None == 0 is', 'False',
        'print(None == 0)\nprint(bool(None))\nprint(bool([]))\nprint(bool(0))', 'False\nFalse\nFalse\nFalse',
        'x = None\nprint(x is None)\nprint(bool("0"))', 'True\nTrue',
        wrong: ['True', 'None', 'Error']),
  ];
}

List<Lesson> stringsDeepTopics() {
  const c = 'course_py_strings_deep';
  return [
    pyTopic('py_str_create_01', c, 'String Creation', 'Quotes make text — single, double, or concat',
        'A string is quotes around characters. `\'` and `"` are the same type. Adjacent strings concatenate at compile time; `+` concatenates at runtime.',
        'IDs, prompts, and paths are strings. A missing quote is a syntax error; a wrong quote in JSON is a data error.',
        'Pick one quote style per file. Use the other to hold the first as data.',
        'Both "a" and \'a\' are', 'str',
        'a = "learn"\nb = "ai"\nprint(a)\nprint(a + b)', 'learn\nlearnai',
        'print("ok")\nprint("a" + "b")', 'ok\nab',
        wrong: ['bytes', 'list', 'int']),
    pyTopic('py_str_idx_01', c, 'String Indexing', 's[0] is the first character — negative counts from the end',
        'Strings are sequences. `s[i]` is one character (a str of length 1). `s[-1]` is the last. Out of range raises.',
        'Off-by-one on a token string is a wrong example, not a crash you will notice in a metric.',
        'Index after you know `len(s)`. Prefer slices for ranges.',
        's[-1] on "cat" is', 't',
        's = "learn"\nprint(s[0])\nprint(s[4])\nprint(s[-1])\nprint(len(s))', 'l\nn\nn\n5',
        'print("ai"[0])\nprint("ai"[-1])', 'a\ni',
        wrong: ['n', 'l', 'learn']),
    pyTopic('py_str_slice_01', c, 'String Slicing', 's[start:stop] — stop is exclusive',
        'A slice copies a substring. Omit start → 0. Omit stop → end. Negative indexes count from the end. Step is not in this sandbox.',
        'Head/tail of a log line is a slice. A wrong stop drops the token you needed.',
        'Write `s[a:b]` and print `len` of the result until it matches the mental picture.',
        '"learn"[1:4] is', 'ear',
        's = "learn"\nprint(s[1:4])\nprint(s[:2])\nprint(s[2:])\nprint(s[-3:])', 'ear\nle\narn\narn',
        'print("abcd"[1:3])\nprint("abcd"[:])', 'bc\nabcd',
        wrong: ['lear', 'earn', 'l']),
    pyTopic('py_str_fmt_01', c, 'String Formatting & f-strings', 'Put values into text without + glue',
        'f-strings evaluate `{expr}` inside the string. Format specs like `{x:.2f}` control display. Concatenation is for joining pieces you already have.',
        'A metric log without units is folklore. f-strings make the unit part of the line.',
        'Prefer f-strings over `%` and `.format` for new code.',
        'f"hi {name}" with name="Ada" prints', 'hi Ada',
        'name = "Ada"\nlr = 0.01\nprint(f"hi {name}")\nprint(f"lr={lr}")', 'hi Ada\nlr=0.01',
        'n = 3\nprint(f"{n} epochs")', '3 epochs',
        wrong: ['hi {name}', 'Ada hi', 'name']),
    pyTopic('py_str_esc_01', c, 'Escape Characters', '\\n \\t \\\\ — special chars inside quotes',
        '`\\n` is a newline, `\\t` a tab, `\\\\` a backslash. The sandbox also unescapes `\\\'` and `\\"`.',
        'A path with unescaped backslashes is a Windows tax. A log line without `\\n` glues two records.',
        'Print the escaped and unescaped forms side by side until you see the difference.',
        '\\n in a string means', 'A newline character',
        'print("a\\nb")\nprint("a\\tb")', 'a\nb\na\tb',
        'print("c:\\\\data")', 'c:\\data',
        wrong: ['The two characters backslash and n', 'A tab only', 'End of file']),
    pyTopic('py_str_raw_01', c, 'Raw Strings', 'Treat backslashes as literal characters',
        'A raw string (r"...") keeps backslashes. This sandbox has no r-prefix, so you write the doubled form `\\\\` when you mean one slash.',
        'Windows paths and regex patterns are slash-heavy. A forgotten escape deletes characters.',
        'Prefer `pathlib` over raw path strings when you can.',
        'A raw string is useful when', 'You need literal backslashes (paths, regex)',
        'print("\\\\n")\nprint("\\\\t")', '\\n\n\\t',
        'p = "C:\\\\logs\\\\run.txt"\nprint(p)', 'C:\\logs\\run.txt',
        wrong: ['You want a newline', 'You want to run code', 'You want bytes']),
    pyTopic('py_str_multi_01', c, 'Multiline Strings', 'More than one line of text in one value',
        'Triple quotes make a multiline string. This sandbox uses `\\n` inside a normal string to the same effect.',
        'Prompts, SQL, and error templates are multiline. Indentation inside the quotes is data.',
        'Build the string, then print it. Watch accidental leading spaces.',
        'A newline inside a string is written as', '\\n',
        'msg = "line1\\nline2\\nline3"\nprint(msg)', 'line1\nline2\nline3',
        'print("a\\n\\nb")', 'a\n\nb',
        wrong: ['/n', '<br>', 'ENTER as a function']),
    pyTopic('py_str_search_01', c, 'String Searching & Replacing', 'find, count, replace, startswith',
        '`find` returns the first index or -1. `count` tallies non-overlapping hits. `replace` returns a new string. `startswith`/`endswith` are bools.',
        'Cleaning labels is search-and-replace. A greedy replace can destroy a token you still needed.',
        'Search first, replace second. Print both strings.',
        '"banana".count("a") is', '3',
        's = "banana"\nprint(s.find("an"))\nprint(s.count("a"))\nprint(s.replace("a", "o"))\nprint(s.startswith("ba"))', '1\n3\nbonono\nTrue',
        'print("train.csv".endswith(".csv"))\nprint("ab".find("z"))', 'True\n-1',
        wrong: ['2', '6', '0']),
    pyTopic('py_str_split_01', c, 'String Splitting & Joining', 'split makes a list; join makes a string',
        '`split` cuts on a separator (whitespace if omitted). `join` is called on the separator: `",".join(parts)`.',
        'CSV-ish lines and token lists move back and forth here. A wrong separator drops a column.',
        'Split, transform, join. Keep the separator in one place.',
        '"a,b".split(",") is', "['a', 'b']",
        'print("a,b,c".split(","))\nprint("-".join(["a", "b"]))\nprint("  x  ".strip())', '[a, b, c]\na-b\nx',
        'parts = "one two".split()\nprint(parts)\nprint(" ".join(parts))', '[one, two]\none two',
        wrong: ['"a,b"', 'a b', 'tuple']),
    pyTopic('py_str_uni_01', c, 'Unicode', 'Text is characters, not bytes',
        'Python 3 `str` is Unicode. `len` counts characters, not bytes. Encode to `bytes` only at a socket or file that needs a codec.',
        'A tokenizer vocab is Unicode. Mixing encodings is how names become mojibake.',
        'Keep `str` inside the program. Encode/decode at the edge.',
        'len("café") counts', 'Characters (4), not UTF-8 bytes',
        'print(len("cafe"))\nprint("cafe"[0])\nprint("AI")', '4\nc\nAI',
        'word = "naïve"\nprint(len("naive"))', '5',
        wrong: ['Always 1', 'UTF-16 code units only', 'Pixels']),
  ];
}
