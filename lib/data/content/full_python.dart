import '../../shared/models/content_models.dart';
import 'lesson_factory.dart';

/// Full Python Language Core — one topic per lesson.
List<Lesson> pythonLanguageCoreLessons() {
  const t = 'track_python';
  const m = 'mod_py_core';
  return [
    unit(
      id: 'py_syntax_01',
      trackId: t,
      moduleId: m,
      title: 'Python Syntax',
      subtitle: 'Print, comments, and indentation that is the block',
      what:
          'Python programs are statements from top to bottom. `#` starts a comment. Blocks are indentation, not braces. `print` writes to stdout. A colon starts a suite (`if`, `def`, `for`).',
      why:
          'A single space off and the program is a different program. Every later topic assumes you can read this layout.',
      how:
          'Use 4 spaces. Do not mix tabs. Keep one statement per line until you know why you would not.',
      quiz: 'What starts a new indented block?',
      correct: 'A colon at the end of a compound statement',
      wrong: const ['A semicolon', 'A # comment', 'A GPU flag'],
      eli5: 'Recipes are numbered steps. Python uses spaces instead of boxes around the steps.',
      analogy: 'An outline in a notes app: indent means “this belongs under the line above.”',
      minutes: 12,
    ),
    unit(
      id: 'py_types_01',
      trackId: t,
      moduleId: m,
      title: 'Data Types',
      subtitle: 'int, float, bool, str, None — pick the type before the op',
      what:
          'Every object has a type. Integers count. Floats measure. Bools branch. Strings hold text. None means missing. `type(x)` names the type. Convert at I/O: `int("3")`, `str(3)`.',
      why:
          'Joining a string id to an int id looks equal to you and unequal to Python. That is a silent empty join.',
      how:
          'Print `type` when a value looks “wrong.” Convert once at the edge. Do not compare `"1"` to `1`.',
      quiz: 'int("3") + 1 is',
      correct: '4',
      wrong: const ['"31"', '13', 'An error always'],
      minutes: 12,
    ),
    unit(
      id: 'py_core_01',
      trackId: t,
      moduleId: m,
      title: 'Variables, Names & Objects',
      subtitle: 'A name is a sticky note on an object, not a box of bytes',
      what:
          'In Python a variable is a name bound to an object. Assignment never copies a value into a fixed slot the way C does — it points the name at an object. Integers, floats, bools, None, and strings are the objects you touch every day in AI scripts.',
      why:
          'Silent identity bugs (two names, one list) corrupt labels, caches, and training configs. If you do not know whether a name shares an object, you cannot debug a pipeline.',
      how:
          'Use `id()`, `is`, and `==` on purpose. `is` tests identity (same object). `==` tests equality (same value). Rebind a name with `=` ; mutate an object with methods. Type-check at I/O boundaries with `isinstance` and hints.',
      quiz: 'What does a Python variable store?',
      correct: 'A name bound to an object',
      wrong: const ['A fixed C memory address forever', 'Only numbers', 'The GPU device index'],
      eli5: 'A coat-check ticket points at a coat. Two tickets can point at the same coat.',
      analogy: 'Sticky notes on boxes in a warehouse. Moving a note does not clone the box.',
      code: 'x = 3\ny = x\nx = 4\nprint(y)',
      expected: '3',
      mistakes: const [
        'Thinking assignment copies a list; `b = a` shares the list.',
        'Using `is` to compare strings or numbers you computed.',
        'Mutating a default argument or a shared config object.',
      ],
      takeaways: const [
        'Names bind to objects; assignment rebinds the name.',
        'Integers are immutable, so rebinding x does not change y.',
        'Check types at the edges of your program.',
      ],
      minutes: 14,
      xp: 60,
    ),
    unit(
      id: 'py_int_01',
      trackId: t,
      moduleId: m,
      title: 'Integers & Arithmetic',
      subtitle: 'Whole numbers, floors, and why // is not /',
      what:
          'Integers (`int`) are arbitrary-precision whole numbers. `/` always returns a float. `//` is floor division. `%` is remainder. `**` is power. Bitwise `& | ^ << >>` show up in hashing and flags.',
      why:
          'Label IDs, batch sizes, epoch counts, and token positions are integers. Mixing `/` and `//` silently turns an index into a float and crashes a gather.',
      how:
          'Use `//` for index math. Convert with `int()` only when the fractional part is meaningless. Watch negative floor division (`-5 // 2 == -3` in Python).',
      quiz: 'What is 7 // 2 in Python?',
      correct: '3',
      wrong: const ['3.5', '4', '2'],
      eli5: 'How many whole apples if you split 7 apples between 2 people? Three, and one left over.',
      analogy: 'A ticket counter that only prints whole tickets — never half a seat.',
      code: 'print(7 / 2)\nprint(7 // 2)\nprint(7 % 2)',
      expected: '3.5\n3\n1',
      takeaways: const ['/ is true division.', '// is floor division.', 'Keep IDs as int, not float.'],
      minutes: 12,
    ),
    unit(
      id: 'py_float_01',
      trackId: t,
      moduleId: m,
      title: 'Floats & Precision',
      subtitle: 'Binary fractions are not decimal money',
      what:
          'Floats are IEEE-754 binary approximations. `0.1 + 0.2` is not exactly `0.3`. Special values `inf` and `nan` appear when a loss explodes. Rounding is a display choice, not exact math.',
      why:
          'Learning rates, probabilities, and metrics are floats. Comparing them with `==` flakes tests. A NaN in a gradient poisons a whole run.',
      how:
          'Compare with a tolerance (`abs(a-b) < 1e-9`) or round only for display. Guard against divide-by-zero. Log `isnan` checks on losses.',
      quiz: 'Why is 0.1 + 0.2 == 0.3 often False?',
      correct: 'Binary floating point cannot represent those decimals exactly',
      wrong: const ['Python forbids decimals', 'The CPU is broken', 'You must use integers'],
      eli5: 'Some fractions (like 1/3 in decimal) never terminate. 0.1 is that kind of fraction in binary.',
      analogy: 'Measuring with a ruler marked in halves — 0.1 cm is a guess between ticks.',
      code: 'print(round(0.1 + 0.2, 1))\nprint(0.1 + 0.2 != 0.3)',
      expected: '0.3\nTrue',
      takeaways: const ['Never == floats from math.', 'NaN and inf are real failure modes.', 'Round for humans, tolerate for tests.'],
      minutes: 13,
    ),
    unit(
      id: 'py_bool_01',
      trackId: t,
      moduleId: m,
      title: 'Booleans & Truthiness',
      subtitle: 'True, False, and the values that pretend to be them',
      what:
          '`True` and `False` are a subclass of `int` (1 and 0). Truthiness is broader: `None`, `0`, `0.0`, `""`, `[]`, `{}`, `set()` are falsey. Everything else is truthy — including `"False"` and `[0]`.',
      why:
          '`if scores:` is False for an empty list and True for `[0.0]`. Feature flags and empty batches fail here.',
      how:
          'Prefer explicit checks: `if xs is None`, `if len(xs) == 0`, `if flag is True`. Use `and`/`or` for short-circuit, not for values you will store.',
      quiz: 'Which value is falsey?',
      correct: 'An empty list []',
      wrong: const ['[0]', '"False"', '1'],
      eli5: 'Empty pockets count as “no.” A pocket with a zero-dollar coupon still counts as “yes, there is a thing.”',
      analogy: 'A mailbox: empty means no mail. A letter that says “nothing” is still mail.',
      code: 'print(bool([]), bool([0]), bool(""), bool("False"))',
      expected: 'False True False True',
      takeaways: const ['Empty collections are falsey.', 'Non-empty [0] is truthy.', 'Be explicit at boundaries.'],
      minutes: 12,
    ),
    unit(
      id: 'py_none_01',
      trackId: t,
      moduleId: m,
      title: 'None & Optional Values',
      subtitle: 'The object that means “no value”',
      what:
          '`None` is a singleton. It is not `0`, not `""`, not `False` — though all are falsey. Functions return `None` if they have no `return`. Optional config fields are `T | None`.',
      why:
          'Missing embeddings, missing labels, and missing API keys are `None`. Treating them as `0` trains the wrong model.',
      how:
          'Test with `is None` / `is not None`, never `== None`. Do not use `None` as a mutable default. Convert missing data to a sentinel only with a written rule.',
      quiz: 'How should you test for None?',
      correct: 'Use "is None" because None is a singleton',
      wrong: const ['Use == 0', 'Use len(x) == 0', 'Call bool(x) only'],
      eli5: 'An empty plate is not a plate of zero cookies. “No dessert” is its own fact.',
      analogy: 'A reserved parking spot with no car is not the same as a spot holding a toy car of size 0.',
      code: 'x = None\nprint(x == None)\nprint(x == 0)',
      expected: 'True\nFalse',
      takeaways: const ['None ≠ 0 ≠ "".', 'Always `is None`.', 'Missing is a policy, not a zero.'],
      minutes: 11,
    ),
    unit(
      id: 'py_str_01',
      trackId: t,
      moduleId: m,
      title: 'Strings',
      subtitle: 'Immutable sequences of Unicode characters',
      what:
          'A string is an ordered, immutable sequence of Unicode characters. You index and slice it like a list, but you cannot assign `s[0] = "A"`. Concatenation with `+` builds a new string. Adjacent string literals merge at compile time.',
      why:
          'Prompts, documents, labels, JSON keys, and file paths are strings. Mutability mistakes do not apply — identity and encoding mistakes do. A UTF-8 vs Latin-1 mix corrupts RAG chunks.',
      how:
          'Prefer f-strings or `join` over repeated `+` in a loop. Index with `s[i]`, slice with `s[a:b]`. Decode bytes explicitly: `raw.decode("utf-8")`. Never `eval` a string from a user.',
      quiz: 'Why does s[0] = "A" fail on a string?',
      correct: 'Strings are immutable — you must build a new string',
      wrong: const ['Strings cannot be indexed', 'You need a GPU', 'Python forbids the letter A'],
      eli5: 'A printed sentence on paper. You can read letter 3. You cannot erase it; you print a new sentence.',
      analogy: 'A carved stone tablet. To change a word, you carve a new tablet.',
      code: 's = "learn"\nprint(s[0], s[4], len(s))\nprint(s + "!")',
      expected: 'l n 5\nlearn!',
      mistakes: const [
        'Trying to mutate a string in place.',
        'Comparing strings with `is` instead of `==`.',
        'Assuming one byte equals one character.',
      ],
      takeaways: const [
        'Strings are immutable Unicode sequences.',
        'Slice and build new strings; do not assign into them.',
        'Decode bytes with an explicit encoding.',
      ],
      minutes: 14,
      xp: 65,
    ),
    unit(
      id: 'py_str_02',
      trackId: t,
      moduleId: m,
      title: 'String Methods & Formatting',
      subtitle: 'split, strip, replace, and f-strings you will write daily',
      what:
          '`split` and `join` move between text and lists. `strip`/`lstrip`/`rstrip` remove edges. `replace`, `startswith`, `in`, and `find` search. f-strings interpolate values. `format` and `%` still appear in older code.',
      why:
          'Token-like cleaning, log lines, and prompt templates are method chains. A leftover newline in a label (`"cat\\n"`) becomes a new class.',
      how:
          'Strip at ingest. Split only with a stated separator. Use f-strings for logs. For stable templates, named placeholders beat implicit order.',
      quiz: '"a,b,c".split(",") produces',
      correct: "['a', 'b', 'c']",
      wrong: const ["'a,b,c'", "('a','b','c')", '{a,b,c}'],
      eli5: 'A sentence is a necklace. split opens the clasp and lays the beads in a tray. join strings them again.',
      analogy: 'A mail-merge letter: the template stays; names drop into slots.',
      code: 'name = "Ada"\nprint(f"hi {name}")\nprint("cat" + " " + "dog")',
      expected: 'hi Ada\ncat dog',
      takeaways: const ['strip before you store labels.', 'split/join are inverses if you choose the sep.', 'f-strings for readable interpolation.'],
      minutes: 13,
    ),
    unit(
      id: 'py_list_01',
      trackId: t,
      moduleId: m,
      title: 'Lists',
      subtitle: 'Ordered, mutable sequences — the default collection',
      what:
          'A list is an ordered, mutable sequence. Items can be mixed types (avoid that in AI code). `append`, `extend`, `insert`, `pop`, `remove` change the same object. Indexing is 0-based. Two names can share one list.',
      why:
          'Batches, vocab lists, file paths, and hyperparameter sweeps are lists. Accidental shared mutation is the #1 Python data bug.',
      how:
          '`b = a.copy()` or `b = a[:]` when you need a shallow copy. `list(a)` does the same. Nested lists need `copy.deepcopy`. Prefer `append` over `+` in a loop.',
      quiz: 'After a = [1]; b = a; b.append(2); what is a?',
      correct: '[1, 2] because a and b are the same list',
      wrong: const ['[1] because append copies', '[2]', 'An error'],
      eli5: 'A shopping list on the fridge. Anyone with the list can add milk. Photocopy it if you want your own.',
      analogy: 'A shared whiteboard vs a photocopy of the whiteboard.',
      code: 'a = [1, 2, 3]\na = a + [4]\nprint(a[0], a[3], len(a))',
      expected: '1 4 4',
      mistakes: const [
        'Aliasing: `b = a` then mutating b.',
        'Using a list as a default argument.',
        'Looping with range(len(xs)) instead of `for x in xs`.',
      ],
      takeaways: const [
        'Lists are ordered and mutable.',
        'Assignment shares the list; copy when you need a twin.',
        'append in loops; do not build with +.',
      ],
      minutes: 14,
      xp: 65,
    ),
    unit(
      id: 'py_list_02',
      trackId: t,
      moduleId: m,
      title: 'List Slicing & Methods',
      subtitle: 'start:stop:step, sort, and reverse',
      what:
          'Slices `xs[start:stop:step]` return a new list. Omitting start/stop means the end. Negative step reverses. `sort` mutates; `sorted` returns new. `reverse` mutates; `[::-1]` returns new.',
      why:
          'Train/val splits, last-k tokens, and reversing a sequence are slices. Off-by-one on `stop` (exclusive) drops a label.',
      how:
          'Remember stop is exclusive: `xs[0:2]` is two items. `xs[::-1]` copies reversed. Sort with `key=` instead of decorating.',
      quiz: 'What is [10, 20, 30, 40][1:3]?',
      correct: '[20, 30]',
      wrong: const ['[10, 20, 30]', '[20, 30, 40]', '[30]'],
      eli5: 'Cut a sandwich. The knife on the right does not include the next slice’s start.',
      analogy: 'A film strip: start frame inclusive, end frame exclusive — like Python range.',
      code: 'xs = [10, 20, 30, 40]\nprint(xs[1], xs[2])\nprint(len(xs))',
      expected: '20 30\n4',
      takeaways: const ['Stop is exclusive.', 'sort mutates; sorted copies.', 'Slices copy; they do not share.'],
      minutes: 13,
    ),
    unit(
      id: 'py_tup_01',
      trackId: t,
      moduleId: m,
      title: 'Tuples',
      subtitle: 'Fixed records — hashable when items are hashable',
      what:
          'A tuple is an ordered, immutable sequence. You write `(1, 2)` or `1, 2`. A one-item tuple needs a comma: `(1,)`. Tuples can be dict keys if every item is hashable. Unpacking `x, y = pair` is the daily move.',
      why:
          'Shapes `(B, T, D)`, RGB `(r,g,b)`, and “row records” should not grow an accidental fourth field. Immutability makes them safe as keys and config rows.',
      how:
          'Use tuples for fixed-length records and function multiple-returns. Use lists for variable-length collections. Convert with `tuple(xs)` / `list(t)`.',
      quiz: 'Which is a one-element tuple?',
      correct: '(1,)',
      wrong: const ['(1)', '[1,]', '{1}'],
      eli5: 'A sealed envelope with two photos. You can look; you cannot slip a third photo in.',
      analogy: 'A locked pair of coordinates on a map — the point is the pair, not a growing list.',
      code: 'pt = (3, 4)\nx, y = pt\nprint(x + y)\none = (1,)\nprint(one[0])',
      expected: '7\n1',
      mistakes: const [
        'Writing (1) and thinking it is a tuple — it is just 1.',
        'Putting a list inside a tuple and using it as a dict key.',
        'Mutating via a list stored inside a tuple and calling the tuple “immutable” in spirit.',
      ],
      takeaways: const [
        'Tuples are immutable sequences.',
        '(1,) is a tuple; (1) is an int.',
        'Unpack records; do not index magic positions without names.',
      ],
      minutes: 13,
      xp: 60,
    ),
    unit(
      id: 'py_set_01',
      trackId: t,
      moduleId: m,
      title: 'Sets',
      subtitle: 'Unordered unique items — membership in constant time',
      what:
          'A set stores unique hashable items. `{1, 2, 2}` is `{1, 2}`. There is no index: no `s[0]`. Operations: union `|`, intersection `&`, difference `-`, subset `<=`. `in` is average O(1). `frozenset` is immutable and hashable.',
      why:
          'Deduping IDs, vocabularies, allow-lists, and “have we seen this document?” are set problems. A list `in` check on 100k IDs is a silent timeout.',
      how:
          'Build with `set(xs)` to dedup. Iterate only when order does not matter. Need order? Use `dict.fromkeys(xs)` (3.7+ insertion order) or sort.',
      quiz: 'What is len({1, 1, 2})?',
      correct: '2',
      wrong: const ['3', '1', '0'],
      eli5: 'A bag that refuses a second identical marble. You can ask “is the blue one in there?” instantly.',
      analogy: 'A guest list: each name once. You check the door, you do not assign seat numbers.',
      code: 'print(len({1, 1, 2}))\nprint(1 in {1, 2})\nprint(3 in {1, 2})',
      expected: '2\nTrue\nFalse',
      mistakes: const [
        'Expecting a stable index or sort order.',
        'Putting a list inside a set.',
        'Using a list for membership on a large unique collection.',
      ],
      takeaways: const [
        'Sets store unique hashable items.',
        'No indexing — only membership and set algebra.',
        'Dedup with set(); use frozenset as a key.',
      ],
      minutes: 14,
      xp: 65,
    ),
    unit(
      id: 'py_dict_01',
      trackId: t,
      moduleId: m,
      title: 'Dictionaries',
      subtitle: 'Key → value maps — the structure of configs and records',
      what:
          'A dict maps hashable keys to values. `d[k]`, `d.get(k, default)`, `d.keys()`, `d.values()`, `d.items()`. Keys are unique; a later write overwrites. Insertion order is preserved (Python 3.7+).',
      why:
          'JSON, HTTP payloads, hyperparameters, sparse features, and token-to-id tables are dicts. A missing key is either a bug (`d[k]`) or a default (`get`).',
      how:
          'Prefer `.get` at boundaries. Iterate `for k, v in d.items()`. Merge with `{**a, **b}` or `|` (3.9+). Do not mutate a dict while iterating its keys.',
      quiz: 'd.get("z", 0) when "z" is missing returns',
      correct: '0',
      wrong: const ['None always', 'An error', '"z"'],
      eli5: 'A locker wall. The key is the locker number. The value is what is inside. Two bags cannot share one number.',
      analogy: 'A labeled filing cabinet: one folder name, one folder. A new file with the same name replaces the old.',
      code: 'd = {"lr": 0.001, "epochs": 10}\nprint(d["lr"])\nprint("batch" in d)\nprint(len(d))',
      expected: '0.001\nFalse\n2',
      mistakes: const [
        'Using a list as a key.',
        'Assuming .get missing returns an error.',
        'Relying on pre-3.7 dicts being unordered in old stories while writing 3.12 code — still do not use order as a schema if a spec says otherwise.',
      ],
      takeaways: const [
        'Dicts map unique hashable keys to values.',
        '[] raises; get supplies a default.',
        'JSON objects become dicts.',
      ],
      minutes: 14,
      xp: 65,
    ),
    unit(
      id: 'py_dict_02',
      trackId: t,
      moduleId: m,
      title: 'Dict Patterns: Counting, Grouping, Nested',
      subtitle: 'setdefault, counters, and the nested-config trap',
      what:
          'Counting is `d[k] = d.get(k, 0) + 1` or `collections.Counter`. Grouping uses `setdefault(k, []).append(v)`. Nested dicts (`cfg["model"]["lr"]`) need a schema or they grow typos as keys.',
      why:
          'Vocab counts, user→sessions, and YAML-like configs are these patterns. A typo key (`lrr`) is a silent new hyperparameter.',
      how:
          'Use Counter for tallies. Dataclasses for nested configs. `defaultdict(list)` for grouping. Validate keys you accept from JSON.',
      quiz: 'A safe way to increment a count in a dict is',
      correct: 'd[k] = d.get(k, 0) + 1',
      wrong: const ['d[k] += 1 on a missing key without get', 'd.append(k)', 'd + 1'],
      eli5: 'A tally sheet: if the name is new, start at 0, then add one. Do not add one to a blank cell.',
      analogy: 'Cubbies for each student. If the cubby does not exist, build it empty, then drop the paper in.',
      code: 'd = {"a": 3, "b": 2}\nprint(d["a"], d["b"])\nprint(len(d))',
      expected: '3 2\n2',
      takeaways: const ['get-or-zero to count.', 'setdefault or defaultdict to group.', 'Nested dict configs want a schema.'],
      minutes: 13,
    ),
    unit(
      id: 'py_if_01',
      trackId: t,
      moduleId: m,
      title: 'if, elif & else',
      subtitle: 'One branch wins — order matters',
      what:
          '`if` tests a condition. `elif` is another exclusive test. `else` is the rest. Python uses indentation, not braces. Ternary: `a if cond else b`. `match`/`case` (3.10+) handles structured patterns.',
      why:
          'Routing tools, filtering bad rows, and early-return guards are branches. An `elif` in the wrong order makes a dead branch.',
      how:
          'Handle the narrowest or most dangerous case first. Prefer `if not ok: return` guards. Do not nest five levels — extract functions.',
      quiz: 'After if x > 0: ... elif x > 10: ..., when does the elif run?',
      correct: 'Never when x > 10, because the first branch already won',
      wrong: const ['Always for x > 10', 'Only on Tuesdays', 'When x is None'],
      eli5: 'A decision tree on a form. The first true checkbox wins. Later boxes are not asked.',
      analogy: 'Airport security lanes: once you enter lane A, lane B’s sign does not apply.',
      code: 'x = 3\nif x > 10:\n    print("big")\nelse:\n    print("mid")',
      expected: 'mid',
      takeaways: const ['First true branch wins.', 'Indentation is the block.', 'Guard clauses beat deep nests.'],
      minutes: 12,
    ),
    unit(
      id: 'py_for_01',
      trackId: t,
      moduleId: m,
      title: 'for Loops, enumerate & zip',
      subtitle: 'Walk a collection — do not invent indexes',
      what:
          '`for item in xs` walks values. `enumerate(xs, start=0)` adds an index. `zip(a, b)` walks pairs and stops at the shortest. `range(n)` is the integer sequence 0..n-1.',
      why:
          'You still loop I/O, files, and control. Math on arrays should be vectorized — but loops teach the algorithm first.',
      how:
          'Never `for i in range(len(xs))` unless you need the index and values — then `enumerate`. `zip(..., strict=True)` (3.10+) catches length bugs.',
      quiz: 'What should replace range(len(xs)) in most code?',
      correct: 'for x in xs, or enumerate(xs) if you need the index',
      wrong: const ['A goto', 'eval(xs)', 'A second GIL'],
      eli5: 'Walk down a row of lockers. You look at each locker. You only number them if you need the number.',
      analogy: 'A conveyor: items pass you. A clicker in your other hand is enumerate.',
      code: 'print([n * n for n in range(4) if n % 2 == 0])\npairs = list(zip([1, 2], ["a", "b"]))\nprint(pairs[0][0], pairs[0][1])',
      expected: '[0, 4]\n1 a',
      takeaways: const ['Iterate items, not indexes.', 'enumerate when you need both.', 'zip pairs sequences.'],
      minutes: 13,
    ),
    unit(
      id: 'py_while_01',
      trackId: t,
      moduleId: m,
      title: 'while, break & continue',
      subtitle: 'Loop until a condition — and know how it ends',
      what:
          '`while cond:` repeats. `break` leaves the loop. `continue` skips to the next check. An `else` on a loop runs if there was no `break`. Infinite loops need a budget.',
      why:
          'Retry loops, streaming reads, and “until converged” training steps are while-loops. An unbounded retry is a bill.',
      how:
          'Write the stop condition first. Cap retries. Prefer `for` when the collection is known.',
      quiz: 'break inside a while loop',
      correct: 'Exits the loop immediately',
      wrong: const ['Skips one line and continues forever', 'Restarts Python', 'Deletes the condition'],
      eli5: 'Keep scooping ice cream while the bowl is not full. “Break” means you are done even if there is room.',
      analogy: 'A security patrol: walk the hall until the clock hits end-of-shift, or break if the alarm fires.',
      code: 'n = 0\nfor _ in range(3):\n    n = n + 1\nprint(n)',
      expected: '3',
      takeaways: const ['while needs a stop you can prove.', 'break exits; continue skips.', 'Cap retries.'],
      minutes: 11,
    ),
    unit(
      id: 'py_fn_01',
      trackId: t,
      moduleId: m,
      title: 'Functions',
      subtitle: 'A named block with inputs, a return, and a contract',
      what:
          '`def name(args):` defines a reusable block. `return` sends a value; without it the result is `None`. Parameters are local names. Docstrings document the contract.',
      why:
          'Training steps, metrics, and I/O belong in functions you can test. A 400-line script is not a pipeline.',
      how:
          'One job per function. Return values instead of printing when another function will consume them. Type-hint public functions.',
      quiz: 'A function with no return statement returns',
      correct: 'None',
      wrong: const ['0', 'False', 'An error'],
      eli5: 'A recipe card: ingredients in, dish out. If you forget to plate it, the guest gets nothing.',
      analogy: 'A factory station: parts in, a finished piece out, a label on the station.',
      code: 'def add(a, b):\n    return a + b\nprint(add(2, 3))\nprint(add(2, 3) * 2)',
      expected: '5\n10',
      takeaways: const ['def names a contract.', 'return or you get None.', 'Small functions are testable.'],
      minutes: 13,
    ),
    unit(
      id: 'py_fn_02',
      trackId: t,
      moduleId: m,
      title: 'Arguments: defaults, *args, **kwargs',
      subtitle: 'The default list is shared — never do that',
      what:
          'Positional args fill left to right. Defaults bind at define time — a default `[]` is one list for all calls. `*args` collects extras. `**kwargs` collects named extras. Keyword-only args sit after `*`.',
      why:
          '`def f(xs=[])` is a legendary bug: every call shares the same list. Config functions grow `**kwargs` until nothing is typed.',
      how:
          'Use `None` as a default and allocate inside. Spell out parameters you care about. Do not swallow **kwargs unless you forward them on purpose.',
      quiz: 'Why is def f(xs=[]) dangerous?',
      correct: 'The default list is created once and shared across calls',
      wrong: const ['Python forbids lists', 'Lambdas cannot run', 'It disables GPUs'],
      eli5: 'A shared backpack left at the classroom door. Every kid who “got a new bag” is stuffing the same bag.',
      analogy: 'A hotel notepad that is not replaced between guests.',
      code: 'def add(a, b=1):\n    return a + b\nprint(add(2), add(2, 3))',
      expected: '3 5',
      takeaways: const ['Defaults bind once.', 'Never default to a mutable.', '*args/**kwargs are escapes, not a schema.'],
      minutes: 14,
    ),
    unit(
      id: 'py_lambda_01',
      trackId: t,
      moduleId: m,
      title: 'Lambda Expressions',
      subtitle: 'A tiny anonymous function — not a place for logic',
      what:
          '`lambda args: expr` creates a function from one expression. No statements, no annotations. Use for `key=` on sort, or a one-off map. Anything longer should be `def`.',
      why:
          'Interview code and `sort(key=...)` use lambdas. A 12-line lambda is unreadable and untestable.',
      how:
          '`sorted(rows, key=lambda r: r["score"])`. Name a `def` the moment you want a docstring or two lines.',
      quiz: 'A lambda can contain',
      correct: 'A single expression, not a block of statements',
      wrong: const ['Any number of return statements', 'class definitions', 'import statements'],
      eli5: 'A sticky note that says “double this” — not a whole cookbook.',
      analogy: 'A one-button remote. If you need a menu, buy a real remote (a def).',
      code: 'def double(n):\n    return n * 2\nprint(double(5))',
      expected: '10',
      takeaways: const ['Lambda is one expression.', 'Great as a sort key.', 'Promote to def when it grows.'],
      minutes: 11,
    ),
    unit(
      id: 'py_comp_01',
      trackId: t,
      moduleId: m,
      title: 'Comprehensions',
      subtitle: 'List, set, and dict builds in one expression',
      what:
          '`[f(x) for x in xs if p(x)]` builds a list. `{x for x in xs}` a set. `{k: v for k, v in pairs}` a dict. Generator expressions `(x for x in xs)` are lazy. Nested comprehensions get unreadable fast.',
      why:
          'Filtering rows and mapping tokens is clearer as a comprehension than as a 6-line loop — until it is not.',
      how:
          'One `for`, one `if`. If you nest, use a loop. Do not hide side effects (prints, appends to outer lists) inside a comprehension.',
      quiz: '[n*n for n in range(4) if n % 2 == 0] is',
      correct: '[0, 4]',
      wrong: const ['[0, 1, 4, 9]', '[1, 9]', '[0, 2, 4]'],
      eli5: 'A factory line: take each item, skip some, stamp the rest into a new tray.',
      analogy: 'A cookie cutter tray — one motion makes many cookies, but you still pick the dough first.',
      code: 'print([n * n for n in range(4) if n % 2 == 0])\nprint("a" + "b")',
      expected: '[0, 4]\nab',
      takeaways: const ['Comprehensions build collections.', 'Keep them to one filter.', 'Generators are lazy cousins.'],
      minutes: 13,
    ),
    unit(
      id: 'py_oop_01',
      trackId: t,
      moduleId: m,
      title: 'Classes & Objects',
      subtitle: 'Bundle state and behavior under a type',
      what:
          'A class is a blueprint. An instance holds state in `self`. `__init__` constructs. Methods are functions that receive the instance. Attributes are names on the object.',
      why:
          'Models, datasets, and clients are objects so you can swap implementations. A pile of globals is not a trainer.',
      how:
          'Keep `__init__` boring. Do not do I/O in constructors if you can avoid it. One class, one reason to change.',
      quiz: 'The first parameter of an instance method is',
      correct: 'self — the instance the method was called on',
      wrong: const ['cls only', 'the class name as a string', 'a GPU handle'],
      eli5: 'A cookie cutter (class) and the cookies (objects). Each cookie can have its own frosting.',
      analogy: 'A job title vs a person who holds the job — the person has a name and a desk.',
      code: 'def box_inc(n):\n    return n + 1\nprint(box_inc(3))',
      expected: '4',
      takeaways: const ['Class = blueprint; instance = state.', 'self is the instance.', 'Keep constructors simple.'],
      minutes: 14,
    ),
    unit(
      id: 'py_oop_02',
      trackId: t,
      moduleId: m,
      title: 'Inheritance & Dunder Methods',
      subtitle: 'Reuse carefully; teach objects how to print and compare',
      what:
          'A subclass inherits methods. `super()` calls the parent. Dunder methods (`__repr__`, `__eq__`, `__len__`, `__iter__`) hook into Python syntax. Prefer composition when inheritance is only for reuse.',
      why:
          'Frameworks call `__call__` on modules and `__len__` on datasets. A bad `__eq__` breaks tests and sets.',
      how:
          'Implement `__repr__` for every data class. Use dataclasses before writing ten dunders. Deep inheritance trees are a smell.',
      quiz: 'super() is used to',
      correct: 'Call a method on a parent class',
      wrong: const ['Delete the subclass', 'Start a GPU', 'Import numpy'],
      eli5: 'A child recipe that says “do the parent cake, then add sprinkles.”',
      analogy: 'A store-brand charger that still uses the USB rulebook (`__` methods) so any phone can plug in.',
      code: 'def parent():\n    return "P"\ndef child():\n    return parent() + "C"\nprint(child())',
      expected: 'PC',
      takeaways: const ['Inherit for “is-a”, compose for “has-a”.', 'Dunders are the language hooks.', 'repr saves debugging hours.'],
      minutes: 13,
    ),
    unit(
      id: 'py_hint_01',
      trackId: t,
      moduleId: m,
      title: 'Type Hints',
      subtitle: 'Contracts the computer can check before a 4-hour run',
      what:
          'Annotations like `def f(x: int) -> str` document types. They do not enforce at runtime unless you use a checker (mypy, Pyright) or pydantic. `list[int]`, `dict[str, float]`, `X | None` are everyday forms.',
      why:
          'A tensor that was a list of strings fails in the trainer, not in the type checker you skipped. Public APIs need hints.',
      how:
          'Hint public functions and configs. Use `From`/`TypedDict` for JSON. Do not hint every local `i` in a loop.',
      quiz: 'Type hints in CPython by themselves',
      correct: 'Document types; they are not runtime-enforced',
      wrong: const ['Make the GPU faster', 'Reject wrong types at runtime always', 'Replace unit tests'],
      eli5: 'A label on a lunchbox. The label does not lock the box — an inspector (mypy) can still yell.',
      analogy: 'Shipping labels: useful when someone checks them; ignored if nobody reads the dock.',
      code: 'def twice(n: int) -> int:\n    return n * 2\nprint(twice(4))',
      expected: '8',
      takeaways: const ['Hints are contracts for humans and checkers.', 'Runtime still needs validation at edges.', 'Hint public APIs.'],
      minutes: 12,
    ),
    unit(
      id: 'py_mod_01',
      trackId: t,
      moduleId: m,
      title: 'Modules & Imports',
      subtitle: 'A .py file is a module; import gives you its names',
      what:
          'A module is a file. `import math` binds the module. `from math import sqrt` binds one name. `import x as y` renames. Circular imports are a design smell. `if __name__ == "__main__"` guards scripts.',
      why:
          'Training entrypoints, eval scripts, and libraries must import cleanly. Side effects at import time make tests flaky.',
      how:
          'Keep imports at the top. Avoid `from x import *`. Do not run training when the file is imported — guard main.',
      quiz: 'if __name__ == "__main__" is true when',
      correct: 'The file is run as a script, not only imported',
      wrong: const ['Any import of the file', 'Only on Windows', 'Only if numpy is present'],
      eli5: 'A recipe book (module). Importing is photocopying a page. Running the file is cooking the whole meal.',
      analogy: 'A toolbox vs opening the toolbox and immediately starting the drill.',
      code: 'import math\nprint(int(math.sqrt(16)))',
      expected: '4',
      takeaways: const ['Module = file.', 'Guard script side effects.', 'No star imports in library code.'],
      minutes: 12,
    ),
    unit(
      id: 'py_pkg_01',
      trackId: t,
      moduleId: m,
      title: 'Packages',
      subtitle: 'A directory of modules with an import path',
      what:
          'A package is a directory of modules. `__init__.py` (optional in namespace packages) marks it. Import `myproj.data.loader`. Relative imports (`from .x import y`) work inside packages only.',
      why:
          'A real AI repo is a package, not a pile of notebooks. Tests and CI import the package.',
      how:
          'One top-level package name. Put `src/` layouts in pyproject. Do not put scripts that mutate sys.path in production.',
      quiz: 'A package is',
      correct: 'A directory of modules you import by dotted path',
      wrong: const ['A GPU image', 'A single integer', 'A pip version only'],
      eli5: 'A binder with tabbed sections. Each tab is a module. The binder name is the package.',
      analogy: 'A company org chart: Engineering.Data.Loader is a desk, not a loose sticky note.',
      code: 'print("pkg")',
      expected: 'pkg',
      takeaways: const ['Package = importable directory.', 'Dotted paths are the API.', 'Relative imports stay inside the package.'],
      minutes: 11,
    ),
    unit(
      id: 'py_venv_01',
      trackId: t,
      moduleId: m,
      title: 'Virtual Environments',
      subtitle: 'One project, one set of package versions',
      what:
          'A venv is an isolated Python plus site-packages. `python -m venv .venv` then pip install. Requirements/lock files pin versions. System Python is for the OS, not your model.',
      why:
          'NumPy 1.x vs 2.x, CUDA wheels, and “it works on my machine” are environment bugs. Two projects must not share one global pip.',
      how:
          'One venv per project. Pin deps. Never `sudo pip`. Recreate from the lock file in CI.',
      quiz: 'Why use a virtual environment?',
      correct: 'To isolate package versions per project',
      wrong: const ['To make lists faster', 'To disable type hints', 'To replace Git'],
      eli5: 'Each science fair project gets its own box of chemicals. You do not pour them all into the kitchen.',
      analogy: 'A separate toolbox per job site so the 10mm socket is the version that job needs.',
      code: 'print("venv")',
      expected: 'venv',
      takeaways: const ['One project, one venv.', 'Pin and lock dependencies.', 'Never install into system Python for app work.'],
      minutes: 11,
    ),
    unit(
      id: 'py_iter_01',
      trackId: t,
      moduleId: m,
      title: 'Iterators',
      subtitle: 'The next-item protocol behind for-loops',
      what:
          'An iterable can produce an iterator (`iter(xs)`). An iterator has `next()` and raises `StopIteration` when done. `for` calls this protocol. Files, ranges, and dict views are iterables.',
      why:
          'Large corpora should stream. Understanding iterators is how you write datasets that do not load 40GB into RAM.',
      how:
          'Implement `__iter__` / `__next__` only when a generator will not do. Do not exhaust an iterator twice unless you re-create it.',
      quiz: 'A for-loop stops when the iterator',
      correct: 'Raises StopIteration',
      wrong: const ['Returns None forever', 'Calls break automatically always', 'Fills the GPU'],
      eli5: 'A ticket machine that gives the next number. When tickets are gone, it rings a bell (StopIteration).',
      analogy: 'A playlist cursor: next song, next song, then the playlist ends.',
      code: 'xs = [10, 20]\nfor x in xs:\n    print(x)',
      expected: '10\n20',
      takeaways: const ['for uses the iterator protocol.', 'Iterators exhaust.', 'Stream large data.'],
      minutes: 12,
    ),
    unit(
      id: 'py_gen_01',
      trackId: t,
      moduleId: m,
      title: 'Generators',
      subtitle: 'Functions that yield — lazy streams',
      what:
          'A generator function uses `yield`. Each call to `next` resumes. State lives between yields. Generator expressions `(x for x in xs)` are compact lazy maps.',
      why:
          'Log lines, shuffling buffers, and token streams should not materialize as giant lists.',
      how:
          'Yield rows from a file. Do not yield after you already built a full list — that misses the point. Close files in `try/finally` or context managers.',
      quiz: 'A generator function uses which keyword?',
      correct: 'yield',
      wrong: const ['goto', 'defer', 'async only'],
      eli5: 'A storyteller who pauses after each sentence. You say “next” and they continue — they do not recite the whole book first.',
      analogy: 'A faucet, not a filled bathtub.',
      code: 'def g():\n    yield 1\n    yield 2\nprint(list(g()))',
      expected: '[1, 2]',
      takeaways: const ['yield makes a generator.', 'Lazy beats loading everything.', 'list(g()) materializes on purpose.'],
      minutes: 13,
    ),
    unit(
      id: 'py_dec_01',
      trackId: t,
      moduleId: m,
      title: 'Decorators',
      subtitle: 'Wrap a callable without rewriting every call site',
      what:
          'A decorator is a function that takes a function and returns a new one. `@timer` above `def f` is `f = timer(f)`. `functools.wraps` preserves the name. Decorators can take arguments (decorator factories).',
      why:
          'Retry, tracing, auth, and timing wrap many functions. Copy-pasting that wrapper is how logs drift.',
      how:
          'Keep decorators small. Do not hide control flow that callers must understand (like swallowing all errors).',
      quiz: '@deco above def f is the same as',
      correct: 'f = deco(f)',
      wrong: const ['f = f(deco)', 'del f', 'import deco'],
      eli5: 'A gift wrap station. The present is the function. The wrap adds a bow (timing, logging) without changing the present’s job.',
      analogy: 'A security sleeve on an ID badge — same person, extra check at the door.',
      code: 'def f(x):\n    return x\ndef wrap(x):\n    return f(x) + 1\nprint(wrap(3))',
      expected: '4',
      takeaways: const ['@deco means f = deco(f).', 'Use wraps to keep metadata.', 'Do not hide failures in decorators.'],
      minutes: 13,
    ),
    unit(
      id: 'py_file_01',
      trackId: t,
      moduleId: m,
      title: 'Files & Context Managers',
      subtitle: 'open, read, write — and always close',
      what:
          '`open(path, mode)` returns a file. Modes: `r`, `w`, `a`, `b` for bytes. `with open(...) as f:` closes even if you error. `pathlib.Path` is the modern path API. Text needs an encoding.',
      why:
          'Datasets, checkpoints, and logs are files. A leaked handle locks a file on Windows. Wrong encoding corrupts text.',
      how:
          'Always `with`. Prefer `Path.read_text(encoding="utf-8")` for small files. Stream large files line by line.',
      quiz: 'with open(...) as f guarantees',
      correct: 'The file is closed when the block ends, even on error',
      wrong: const ['The file is never created', 'UTF-16 always', 'That JSON is valid'],
      eli5: 'Borrowing a library book with a desk rule: when you leave the desk, the book goes back — even if you spill juice.',
      analogy: 'A hotel key card that stops working when you check out (the `with` block ends).',
      code: 'print("file")',
      expected: 'file',
      takeaways: const ['with closes files.', 'Set encoding for text.', 'Stream large inputs.'],
      minutes: 12,
    ),
    unit(
      id: 'py_json_01',
      trackId: t,
      moduleId: m,
      title: 'JSON',
      subtitle: 'The interchange format of APIs and configs',
      what:
          '`json.loads` parses a string to dicts/lists. `json.dumps` goes the other way. JSON types: object, array, string, number, true/false, null. Python tuples become arrays. Keys must be strings.',
      why:
          'Every model API, every dataset metadata file, every tool call is JSON (or JSON-shaped). `eval` on JSON is a security hole.',
      how:
          'Validate against a schema (pydantic). Never `eval`. Watch `NaN` — standard JSON cannot represent it; some libs cheat.',
      quiz: 'What should you do with untrusted JSON?',
      correct: 'Parse with json and validate against a schema',
      wrong: const ['eval() it', 'Ignore all keys', 'Write it to GPU memory raw'],
      eli5: 'A packing list written in a language both warehouses speak. You do not execute the packing list as code.',
      analogy: 'A customs form: fields and types. A novel is not a customs form.',
      code: 'import json\nprint(json.loads(\'{"k": 1}\')["k"])',
      expected: '1',
      takeaways: const ['loads/dumps, never eval.', 'Validate at the edge.', 'null becomes None.'],
      minutes: 13,
    ),
    unit(
      id: 'py_http_01',
      trackId: t,
      moduleId: m,
      title: 'HTTP for Model APIs',
      subtitle: 'Requests have methods, headers, bodies, and status codes',
      what:
          'HTTP is request/response. GET reads. POST/PUT send bodies. Status 2xx ok, 4xx your fault, 5xx theirs. Headers carry auth and content-type. Timeouts are mandatory.',
      why:
          'Embeddings, chat completions, and vector DBs are HTTP. A hung request without a timeout stalls a worker forever.',
      how:
          'Set timeouts. Retry only idempotent calls with backoff. Log status and a request id. Do not put secrets in query strings.',
      quiz: 'A client should treat HTTP timeouts as',
      correct: 'Required — a hung socket is an incident',
      wrong: const ['Optional for internal APIs', 'A GPU setting', 'Only for GET images'],
      eli5: 'You knock on a door (request). They slide a note back (response). If nobody answers, you do not wait until winter (timeout).',
      analogy: 'A restaurant ticket: you order (POST), they bring a plate (body) and a bill stamp (status).',
      code: 'print("http")',
      expected: 'http',
      takeaways: const ['Timeouts are required.', 'Status codes are part of the API.', 'Retry with a budget.'],
      minutes: 12,
    ),
    unit(
      id: 'py_cli_01',
      trackId: t,
      moduleId: m,
      title: 'Command-Line Tools',
      subtitle: 'Training and eval belong on an argv contract',
      what:
          'CLIs take `sys.argv` or argparse/typer. Exit code 0 means success — CI depends on it. Flags should be explicit: `--data`, `--seed`, `--out`.',
      why:
          'A notebook is not an entrypoint. Nightly jobs and teammates run the same CLI.',
      how:
          'Parse args; do not read hidden env vars for required paths. Print a one-line summary of the run. Exit non-zero on failure.',
      quiz: 'A CLI that fails should',
      correct: 'Exit with a non-zero status code',
      wrong: const ['Always exit 0', 'Only print in yellow', 'Reboot the machine'],
      eli5: 'A light switch for a robot: you tell it which room and which task. If it fails, it leaves the light blinking (exit code).',
      analogy: 'A takeout order slip: the kitchen and the courier both read the same fields.',
      code: 'print("cli")',
      expected: 'cli',
      takeaways: const ['CLIs are the job API.', 'Exit codes matter in CI.', 'Required paths are flags, not vibes.'],
      minutes: 11,
    ),
    unit(
      id: 'py_exc_01',
      trackId: t,
      moduleId: m,
      title: 'Exceptions',
      subtitle: 'Catch the errors you expect; crash on the rest',
      what:
          '`raise` signals failure. `try` / `except` catch it. Catch the narrow type you can handle. A bare `except Exception: pass` hides bad labels and bad files.',
      why:
          'Training jobs that swallow errors write dummy metrics and ship. APIs that do the same return 200 with an empty body.',
      how:
          'Validate at the edge. Catch JSON/parse/timeout. Let unexpected exceptions fail the process.',
      quiz: 'An unknown exception in a training entrypoint should',
      correct: 'Fail the job so you notice',
      wrong: const ['Be ignored', 'Retry forever', 'Write accuracy 1.0'],
      minutes: 13,
    ),
    unit(
      id: 'py_api_01',
      trackId: t,
      moduleId: m,
      title: 'APIs',
      subtitle: 'A contract: request in, JSON out, status if it failed',
      what:
          'An API is a typed contract over the network. You send a method, path, headers, and optional JSON body. You get a status and a body. Timeouts and schema checks are part of the call, not extras.',
      why:
          'Embeddings, chat, and vector search are APIs. A client that does not validate will train on garbage.',
      how:
          'Parse JSON. Check required keys. Set a timeout. Log a request id. Do not eval the body.',
      quiz: 'Before you use an API body you should',
      correct: 'Parse JSON and validate required keys',
      wrong: const ['eval() it', 'Ignore the status', 'Assume it is a list'],
      minutes: 13,
    ),
  ];
}
