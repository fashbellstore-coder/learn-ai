import '../../shared/models/content_models.dart';
import '../../shared/models/enums.dart';
import 'syllabus_foundations.dart';

/// Missing AI-engineer pillars: software engineering, SQL, app APIs, cloud, enterprise.
List<RoadmapTrack> gapTracks() => [
      _swe(),
      _sql(),
      _appdev(),
      _cloud(),
      _enterprise(),
    ];

Lesson _s(
  String track,
  String mod,
  String id,
  String title,
  String sub,
  String what,
  String why,
  String how,
  String quiz,
  String ok,
  String c1,
  String e1,
  String c2,
  String e2,
) {
  return u(id, track, mod, title, sub, what, why, how, quiz, ok,
      code: c1, expected: e1, code2: c2, expected2: e2);
}

RoadmapTrack _swe() {
  const id = 'track_swe';
  const mod = 'mod_swe_01';
  return RoadmapTrack(
    id: id,
    levelNumber: 22,
    title: 'Software Engineering',
    tagline: 'Clean code, DSA, system design, and review',
    icon: '🛠️',
    colorHex: '#1565C0',
    description:
        'An AI engineer ships software. This level is clean code, SOLID, data structures, algorithms, system design, testing, and Git — the craft under every model.',
    keySkills: const ['Clean code', 'SOLID', 'DSA', 'System design', 'Testing', 'Git'],
    prerequisiteTrackIds: const ['track_python'],
    careerGoals: const [CareerGoal.aiEngineer, CareerGoal.mlEngineer, CareerGoal.mlopsEngineer],
    modules: [
      LearningModule(
        id: mod,
        trackId: id,
        title: 'Engineer the software',
        description: 'Code that other humans can change.',
        lessons: [
          _s(id, mod, 'swe_01', 'Clean Code', 'Names, small functions, one job',
              'Clean code is readable by a stranger in six months. Names say what. Functions do one thing. Comments explain why, not what. Dead code is deleted.',
              'A clever one-liner that nobody can change is a liability next to a model card.',
              'Print a good name and a bad name.',
              'A function should', 'Do one job that its name states',
              'print("load_users")\nprint("do_stuff")', 'load_users\ndo_stuff',
              'print("why")\nprint("not what")', 'why\nnot what'),
          _s(id, mod, 'swe_02', 'SOLID Principles', 'Five habits, not a religion',
              'SRP: one reason to change. OCP: extend without editing a pile of ifs. LSP: subtypes keep the contract. ISP: small interfaces. DIP: depend on abstractions you own.',
              'A 2,000-line Trainer class that also logs, emails, and deploys is why Friday breaks Monday.',
              'Print the five initials.',
              'SOLID is', 'Five design habits — use them when the pain is real',
              'print("SRP")\nprint("DIP")', 'SRP\nDIP',
              'letters = ["S", "O", "L", "I", "D"]\nprint(len(letters))\nprint(letters[0])', '5\nS'),
          _s(id, mod, 'swe_03', 'Data Structures', 'Pick the cost you will pay',
              'Lists are arrays. Dicts are hash maps. Sets are membership. Stacks, queues, heaps, and graphs are the rest. Big-O is the bill.',
              'An O(n) lookup in a hot inference loop is a latency incident.',
              'Print list vs set membership intent.',
              'A set is for', 'Fast membership tests, not order',
              'print(1 in [1, 2, 3])\nprint(1 in {1, 2})', 'True\nTrue',
              'print("O(1) avg")\nprint("hash")', 'O(1) avg\nhash'),
          _s(id, mod, 'swe_04', 'Algorithms', 'Correct, then fast enough',
              'Search, sort, two pointers, BFS/DFS, and dynamic programming are the interview set. Production cares about the actual n and the cache.',
              'A wrong algorithm with a GPU is still wrong.',
              'Print binary-search steps vs linear for n=8.',
              'Pick an algorithm by', 'Correctness first, then the real n and memory',
              'print(3)\nprint(8)', '3\n8',
              'print("correct")\nprint("then fast")', 'correct\nthen fast'),
          _s(id, mod, 'swe_05', 'System Design', 'Boxes, queues, and failure',
              'System design is APIs, data stores, caches, queues, and what happens when a box dies. Draw the request path. Name the SLO. Name the single point of failure.',
              'A notebook that “calls the model” is not an architecture.',
              'Print client, api, queue, worker.',
              'A design review should name', 'The request path, the SLO, and the failure mode',
              'path = ["client", "api", "queue", "worker"]\nprint(len(path))\nprint(path[2])', '4\nqueue',
              'print("SLO")\nprint("SPOF")', 'SLO\nSPOF'),
          _s(id, mod, 'swe_06', 'Testing', 'A failing test is a gift',
              'Unit tests pin a function. Integration tests pin a boundary. Fixtures are data. Flakes are bugs. Coverage is a hint, not a trophy.',
              'An untested prompt parser will ship a jailbreak as a feature.',
              'Print unit vs integration.',
              'A unit test should', 'Pin one function without the network',
              'print("unit")\nprint("integration")', 'unit\nintegration',
              'print("flake")\nprint("bug")', 'flake\nbug'),
          _s(id, mod, 'swe_07', 'Code Review', 'The PR is the product',
              'Review for correctness, security, and clarity. Nit style is for the formatter. A review that only says LGTM taught nobody.',
              'Models do not review your authz filter.',
              'Print ask, risk, and test.',
              'A useful review comments on', 'Correctness, security, and a missing test',
              'print("correct")\nprint("authz")', 'correct\nauthz',
              'print("test")\nprint("clarity")', 'test\nclarity'),
          _s(id, mod, 'swe_08', 'Git & Documentation', 'History you can trust',
              'Commits are stories. Branches are experiments. PRs are reviews. Docs tell the next human the contract. A README that lies is worse than none.',
              'An undocumented RAG chunker will be rewritten, badly, twice.',
              'Print commit, branch, and pr.',
              'A commit message should', 'Say why the change exists',
              'print("why")\nprint("not how")', 'why\nnot how',
              'print("README")\nprint("contract")', 'README\ncontract'),
        ],
      ),
    ],
  );
}

RoadmapTrack _sql() {
  const id = 'track_sql';
  const mod = 'mod_sql_01';
  return RoadmapTrack(
    id: id,
    levelNumber: 23,
    title: 'SQL & Databases',
    tagline: 'From your first SELECT to window functions and query performance',
    icon: 'SQ',
    colorHex: '#336791',
    description:
        'Core SQL from first principles: filtering and sorting, aggregation, joins, subqueries and CTEs, window functions, and the DML/DDL, transactions, and indexing basics behind every data-driven feature — 6 modules, each with a quiz.',
    keySkills: const ['SQL', 'Joins', 'Aggregation', 'Window functions', 'CTEs'],
    prerequisiteTrackIds: const ['track_ds'],
    careerGoals: const [CareerGoal.aiEngineer, CareerGoal.mlEngineer, CareerGoal.nlpEngineer],
    modules: [
      LearningModule(
        id: mod,
        trackId: id,
        title: 'Query the truth',
        description: 'A query is a contract.',
        lessons: [
          _s(id, mod, 'sql_01', 'SQL Fundamentals', 'Tables, rows, and a schema',
              'A table is named columns plus rows. A schema is the contract. SQL is declarative: you say what, the planner says how.',
              'A silent schema change is a silent model feature change.',
              'Print table and two columns.',
              'SQL is', 'A declarative language over tables',
              't = {"name": "users", "cols": ["id", "email"]}\nprint(t["name"])\nprint(len(t["cols"]))', 'users\n2',
              'print("schema")\nprint("contract")', 'schema\ncontract'),
          _s(id, mod, 'sql_02', 'SELECT INSERT UPDATE DELETE', 'Read and write are different privileges',
              'SELECT projects. INSERT adds. UPDATE mutates. DELETE removes. WHERE is the safety. Without WHERE, UPDATE is an incident.',
              'A training job that UPDATEs labels in place with no WHERE will ruin the set.',
              'Print the four verbs.',
              'UPDATE without WHERE', 'Mutates every row — treat it as an incident',
              'print("SELECT")\nprint("UPDATE")', 'SELECT\nUPDATE',
              'print("WHERE")\nprint("safety")', 'WHERE\nsafety'),
          _s(id, mod, 'sql_03', 'JOINs', 'The join is the model',
              'INNER keeps matches. LEFT keeps the left side. Keys must be clean. A bad join explodes rows or drops labels.',
              'A many-to-many join without an aggregate duplicates the target.',
              'Print inner vs left.',
              'A JOIN without a key contract can', 'Explode rows or drop labels silently',
              'print("INNER")\nprint("LEFT")', 'INNER\nLEFT',
              'print("key")\nprint("cardinality")', 'key\ncardinality'),
          _s(id, mod, 'sql_04', 'GROUP BY', 'Split, apply, combine in SQL',
              'GROUP BY buckets rows. Aggregates (COUNT, SUM, AVG) collapse each bucket. HAVING filters after the group. SELECT * with GROUP BY is a bug.',
              'Feature tables are almost always group-then-join.',
              'Print group key and count.',
              'HAVING filters', 'Groups after aggregation',
              'print("user_id")\nprint(3)', 'user_id\n3',
              'print("HAVING")\nprint("after")', 'HAVING\nafter'),
          _s(id, mod, 'sql_05', 'Subqueries & CTEs', 'Name the step, then compose',
              'A subquery is a query in a query. A CTE (WITH) names a step. Prefer CTEs you can read. Correlated subqueries can be O(n²).',
              'An unreadable 80-line nest is how a leak hides.',
              'Print with and as.',
              'A CTE is for', 'Naming a step so the query stays readable',
              'print("WITH")\nprint("AS")', 'WITH\nAS',
              'print("name")\nprint("compose")', 'name\ncompose'),
          _s(id, mod, 'sql_06', 'Window Functions', 'Aggregate without collapsing rows',
              'OVER (PARTITION BY … ORDER BY …) computes rank, lag, rolling sums while keeping every row. This is leakage-aware feature SQL.',
              'A global AVG() in a feature query leaks the future if time is wrong.',
              'Print rank and lag.',
              'A window function', 'Computes over a frame without collapsing rows',
              'print("RANK")\nprint("LAG")', 'RANK\nLAG',
              'print("PARTITION")\nprint("ORDER")', 'PARTITION\nORDER'),
          _s(id, mod, 'sql_07', 'Indexes & Query Plans', 'The planner is the runtime',
              'An index is a side structure that makes a lookup cheap. EXPLAIN shows the plan. A sequential scan on 200M rows is a product outage.',
              'You cannot optimize a query you have not explained.',
              'Print index and explain.',
              'An index helps when', 'The query actually uses it — check EXPLAIN',
              'print("INDEX")\nprint("EXPLAIN")', 'INDEX\nEXPLAIN',
              'print("seq scan")\nprint("bad")', 'seq scan\nbad'),
          _s(id, mod, 'sql_08', 'PostgreSQL, Redis & NoSQL', 'Pick the store for the access pattern',
              'Postgres is the default relational store. Redis is a cache and a queue, not a source of truth. Document/KV stores fit schemaless events. Vector DBs are a later level.',
              'Putting the only copy of user data in Redis is a disaster-recovery story.',
              'Print postgres, redis, and document.',
              'Redis should be treated as', 'A cache or queue — not the system of record',
              'print("postgres")\nprint("redis")', 'postgres\nredis',
              'print("cache")\nprint("not truth")', 'cache\nnot truth'),
        ],
      ),
    ],
  );
}

RoadmapTrack _appdev() {
  const id = 'track_appdev';
  const mod = 'mod_app_01';
  return RoadmapTrack(
    id: id,
    levelNumber: 24,
    title: 'AI Application Development',
    tagline: 'FastAPI, auth, streaming, jobs, and API security',
    icon: '📲',
    colorHex: '#E65100',
    description:
        'Turn a model into a product: REST, FastAPI, auth, WebSockets, streaming tokens, uploads, background jobs, caches, rate limits, and microservices.',
    keySkills: const ['FastAPI', 'Auth', 'WebSockets', 'Streaming', 'Jobs', 'Rate limits'],
    prerequisiteTrackIds: const ['track_python', 'track_llm'],
    careerGoals: const [CareerGoal.aiEngineer, CareerGoal.llmEngineer, CareerGoal.agentEngineer],
    modules: [
      LearningModule(
        id: mod,
        trackId: id,
        title: 'Ship the API',
        description: 'The model is behind a contract.',
        lessons: [
          _s(id, mod, 'app_01', 'REST APIs', 'Resources, verbs, and status codes',
              'REST maps HTTP verbs to resources. 2xx succeeded. 4xx the client was wrong. 5xx you were wrong. JSON is a contract, not a vibe.',
              'A 200 with an error string is how every client bugs twice.',
              'Print get and 404.',
              'A 4xx status means', 'The client request was wrong',
              'print("GET")\nprint(404)', 'GET\n404',
              'print("JSON")\nprint("contract")', 'JSON\ncontract'),
          _s(id, mod, 'app_02', 'FastAPI', 'Types in, types out',
              'FastAPI binds path, query, and body to types. Pydantic validates. OpenAPI is generated. Dependency injection is how auth and DB sessions enter a route.',
              'A raw dict handler is how you ship an untyped LLM wrapper.',
              'Print fastapi and pydantic.',
              'FastAPI request bodies should be', 'Pydantic models, not raw dicts',
              'print("FastAPI")\nprint("Pydantic")', 'FastAPI\nPydantic',
              'print("OpenAPI")\nprint("deps")', 'OpenAPI\ndeps'),
          _s(id, mod, 'app_03', 'Flask vs FastAPI', 'Pick for the job, not the tweet',
              'Flask is minimal and sync-first. FastAPI is async-first with types. Both can serve a model. Neither replaces a queue for a 40-second job.',
              'Blocking a worker on a 30s generate is how you DDoS yourself.',
              'Print async vs sync.',
              'A 40-second generate should', 'Run as a job or stream — not block the only worker',
              'print("async")\nprint("sync")', 'async\nsync',
              'print("queue")\nprint("not block")', 'queue\nnot block'),
          _s(id, mod, 'app_04', 'Authentication & Authorization', 'Who you are vs what you may do',
              'Authn proves identity (token, session). Authz checks a permission (role, tenant, resource). The model does not decide tenancy.',
              'A prompt that says “only this user” is not authz.',
              'Print authn and authz.',
              'Authorization belongs', 'In the API and the data filter, not the prompt',
              'print("authn")\nprint("authz")', 'authn\nauthz',
              'print("token")\nprint("role")', 'token\nrole'),
          _s(id, mod, 'app_05', 'WebSockets & Streaming', 'Tokens leave as they are born',
              'Streaming is SSE or WebSocket chunks. TTFT is the first byte. A buffered 20s wait feels broken even if TPOT is fine.',
              'Users abandon a spinner. They stay for a stream.',
              'Print sse and first token.',
              'Streaming should start on', 'The first token, not the full completion',
              'print("SSE")\nprint("WS")', 'SSE\nWS',
              'print("TTFT")\nprint("first byte")', 'TTFT\nfirst byte'),
          _s(id, mod, 'app_06', 'Uploads & Background Jobs', 'Files and work that outlive the request',
              'Uploads need size limits, type checks, and a virus/PII story. Jobs (Celery, RQ, ARQ) outlive HTTP. The request returns a job id.',
              'Holding a 200MB PDF in RAM on the API box is an OOM.',
              'Print upload and job_id.',
              'A long task should return', 'A job id the client can poll or subscribe to',
              'print("upload")\nprint("job_id")', 'upload\njob_id',
              'print("limit")\nprint("scan")', 'limit\nscan'),
          _s(id, mod, 'app_07', 'Caching & Rate Limits', 'Protect the GPU with boring gates',
              'Cache stable prefixes and identical queries. Rate-limit by user and IP. 429 is a feature. A cache of completions must key on model + prompt pin + params.',
              'One unpaid loop can empty a GPU month.',
              'Print cache key parts and 429.',
              'A completion cache key must include', 'Model id, prompt pin, and sampling params',
              'print("model")\nprint("pin")', 'model\npin',
              'print(429)\nprint("rate")', '429\nrate'),
          _s(id, mod, 'app_08', 'API Security & Microservices', 'Trust boundaries, not more YAML',
              'Validate input. Time out. Do not pass raw user text to a shell. Split ingest, retrieve, and generate if they scale differently. A microservice is a team and a SLO, not a folder.',
              'An unsanitized filename in a tool call is RCE.',
              'Print validate and timeout.',
              'A microservice should exist when', 'It has its own SLO and scaling story',
              'print("validate")\nprint("timeout")', 'validate\ntimeout',
              'print("SLO")\nprint("boundary")', 'SLO\nboundary'),
        ],
      ),
    ],
  );
}

RoadmapTrack _cloud() {
  const id = 'track_cloud';
  const mod = 'mod_cld_01';
  return RoadmapTrack(
    id: id,
    levelNumber: 25,
    title: 'Cloud & Infrastructure',
    tagline: 'Linux, Docker, Kubernetes, CI/CD, and GPUs',
    icon: '☁️',
    colorHex: '#37474F',
    description:
        'The machines under the model: Linux, networks, Docker, Kubernetes, Helm, GitHub Actions, AWS/Azure/GCP, GPU nodes, load balancers, storage, and secrets.',
    keySkills: const ['Linux', 'Docker', 'Kubernetes', 'CI/CD', 'GPUs', 'Secrets'],
    prerequisiteTrackIds: const ['track_python'],
    careerGoals: const [CareerGoal.mlopsEngineer, CareerGoal.aiEngineer],
    modules: [
      LearningModule(
        id: mod,
        trackId: id,
        title: 'Run the machines',
        description: 'Boxes, images, and pipelines.',
        lessons: [
          _s(id, mod, 'cld_01', 'Linux', 'The OS your job actually runs on',
              'Processes, files, permissions, pipes, and logs. systemd or a container entrypoint starts your server. Disk full is a production incident.',
              'If you cannot read a log on the box, you cannot debug the model.',
              'Print ps and chmod.',
              'A full disk is', 'A production incident, not a cleanup later',
              'print("ps")\nprint("chmod")', 'ps\nchmod',
              'print("logs")\nprint("disk")', 'logs\ndisk'),
          _s(id, mod, 'cld_02', 'Networking Basics', 'DNS, ports, TLS, and latency',
              'A request hits DNS, then a port, then TLS, then your process. Timeouts need a budget. Private vs public subnets matter for GPU boxes.',
              'An open Jupyter port on 0.0.0.0 is a breach.',
              'Print dns, tls, and port.',
              'TLS terminates', 'At a load balancer or the process — know which',
              'print("DNS")\nprint("TLS")', 'DNS\nTLS',
              'print(443)\nprint("timeout")', '443\ntimeout'),
          _s(id, mod, 'cld_03', 'Docker', 'The unit of deploy',
              'A Dockerfile is a recipe. An image is a snapshot. A container is a running process with limits. Pin base digests. Do not bake secrets into layers.',
              'latest is not a version.',
              'Print image and pin.',
              'A production image should', 'Pin a digest and exclude secrets',
              'print("image")\nprint("digest")', 'image\ndigest',
              'print("no secrets")\nprint("layer")', 'no secrets\nlayer'),
          _s(id, mod, 'cld_04', 'Kubernetes & Helm', 'Desired state, not SSH',
              'Pods, Deployments, Services, and probes. Helm charts parameterize YAML. A GPU node needs a device plugin and a request you can actually schedule.',
              'Restarting a pod is not a rollback strategy if the image tag moved.',
              'Print pod and probe.',
              'A Kubernetes deploy should pin', 'An image digest and resource requests',
              'print("pod")\nprint("probe")', 'pod\nprobe',
              'print("Helm")\nprint("GPU")', 'Helm\nGPU'),
          _s(id, mod, 'cld_05', 'CI/CD & GitHub Actions', 'Every merge is a candidate',
              'CI runs tests and evals. CD promotes an artifact. Actions are YAML plus secrets. A workflow that deploys on push to main without a gate is a footgun.',
              'A green unit test is not a model quality gate.',
              'Print ci and gate.',
              'CD should promote', 'A built artifact that already passed the gates',
              'print("CI")\nprint("CD")', 'CI\nCD',
              'print("gate")\nprint("artifact")', 'gate\nartifact'),
          _s(id, mod, 'cld_06', 'AWS Azure GCP', 'Three clouds, same shapes',
              'Compute, object storage, IAM, and a managed GPU story exist on all three. Learn the shapes, then the names. Lock the account. Least privilege.',
              'A root key in a notebook is a career event.',
              'Print compute, storage, and iam.',
              'Cloud IAM should follow', 'Least privilege — not a shared admin key',
              'print("compute")\nprint("storage")', 'compute\nstorage',
              'print("IAM")\nprint("least")', 'IAM\nleast'),
          _s(id, mod, 'cld_07', 'GPU Infrastructure', 'Schedulers, drivers, and fragmentation',
              'A GPU node needs a driver, a runtime, and a scheduler that understands MIG or whole cards. Fragmentation looks like “we have GPUs but cannot place the pod.”',
              'Buying A100s does not fix a bad batcher.',
              'Print driver and request.',
              'GPU scheduling fails when', 'Requests do not match how cards are sliced',
              'print("driver")\nprint("request")', 'driver\nrequest',
              'print("MIG")\nprint("place")', 'MIG\nplace'),
          _s(id, mod, 'cld_08', 'Load Balancing, Storage & Secrets', 'Traffic, disks, and keys',
              'LBs spread connections and terminate TLS. Object storage holds artifacts. Block disks hold indexes. Secrets live in a manager, not env files in git.',
              'A vector index on ephemeral disk disappears at preemption.',
              'Print lb, s3, and vault.',
              'Secrets should live in', 'A secrets manager, not the image or git',
              'print("LB")\nprint("object")', 'LB\nobject',
              'print("vault")\nprint("not git")', 'vault\nnot git'),
        ],
      ),
    ],
  );
}

RoadmapTrack _enterprise() {
  const id = 'track_enterprise';
  const mod = 'mod_ent_01';
  return RoadmapTrack(
    id: id,
    levelNumber: 26,
    title: 'Enterprise AI Engineering',
    tagline: 'Private LLMs, SSO, tenancy, HA, and GPU clusters',
    icon: '🏛️',
    colorHex: '#4A148C',
    description:
        'AI behind a firewall: on-prem and air-gapped deploys, private models, LDAP/SSO, RBAC, audit logs, multi-tenancy, high availability, and serving clusters.',
    keySkills: const ['Private LLMs', 'SSO', 'RBAC', 'Audit', 'HA/DR', 'GPU clusters'],
    prerequisiteTrackIds: const ['track_eval_security', 'track_cloud'],
    careerGoals: const [CareerGoal.aiEngineer, CareerGoal.mlopsEngineer],
    modules: [
      LearningModule(
        id: mod,
        trackId: id,
        title: 'Ship behind the firewall',
        description: 'Compliance is a runtime.',
        lessons: [
          _s(id, mod, 'ent_01', 'On-Premises AI', 'The model stays in the building',
              'On-prem means your GPUs, your network, your patches. Vendors may still phone home. An air-gap is stricter: no outbound at all.',
              'A “private” SaaS with your weights in their VPC is not on-prem.',
              'Print onprem and no saas.',
              'On-prem AI means', 'You run the GPUs and the network path',
              'print("on-prem")\nprint("our GPUs")', 'on-prem\nour GPUs',
              'print("patch")\nprint("us")', 'patch\nus'),
          _s(id, mod, 'ent_02', 'Air-Gapped AI', 'No outbound, including “just Hugging Face”',
              'Air-gapped sites cannot pull images or tokenizers at runtime. You vendor every artifact. Updates are a sneakernet or a blessed diode.',
              'from_pretrained at serve time is an outage — and a policy violation.',
              'Print offline and pin.',
              'An air-gapped serve must', 'Use pre-vendored images and weights — no runtime download',
              'print("offline")\nprint("vendored")', 'offline\nvendored',
              'print("no HF")\nprint("at boot")', 'no HF\nat boot'),
          _s(id, mod, 'ent_03', 'Private LLMs', 'Weights you can name in a contract',
              'A private LLM is a pinned model you host. License, eval, and data-retention terms matter more than the blog name. Distill if the box is small.',
              'An API key to a public model is not a private LLM.',
              'Print pin and license.',
              'A private LLM is', 'A pinned, hosted model with a license you accepted',
              'print("pinned")\nprint("hosted")', 'pinned\nhosted',
              'print("license")\nprint("eval")', 'license\neval'),
          _s(id, mod, 'ent_04', 'LDAP SSO & RBAC', 'Identity from the company directory',
              'SSO (SAML/OIDC) is how employees sign in. LDAP/SCIM syncs groups. RBAC maps groups to actions. The model never invents a role.',
              'A shared “ai-admin” password is an audit finding.',
              'Print sso and role.',
              'Roles should come from', 'SSO groups mapped to RBAC — not a spreadsheet in the app',
              'print("SSO")\nprint("RBAC")', 'SSO\nRBAC',
              'print("SAML")\nprint("OIDC")', 'SAML\nOIDC'),
          _s(id, mod, 'ent_05', 'Data Governance & Audit Logs', 'Who saw which document',
              'Governance is classification, retention, and legal hold. Audit logs record access, prompts (redacted), and admin acts. Immutable storage is the point.',
              '“We think legal used the chatbot” is not an audit trail.',
              'Print retain and audit.',
              'An audit log must record', 'Who accessed what, when — with redaction',
              'print("retain")\nprint("audit")', 'retain\naudit',
              'print("who")\nprint("when")', 'who\nwhen'),
          _s(id, mod, 'ent_06', 'Multi-Tenancy', 'Tenant B is not a neighbor, it is a breach',
              'Tenancy is isolation: data, indexes, keys, and quotas. A shared vector collection with a “please filter” is not isolation.',
              'Cosine does not understand contracts.',
              'Print tenant and isolate.',
              'Tenant isolation must be', 'Enforced in storage and query, not the prompt',
              'print("tenant")\nprint("isolate")', 'tenant\nisolate',
              'print("index")\nprint("quota")', 'index\nquota'),
          _s(id, mod, 'ent_07', 'High Availability & Disaster Recovery', 'RPO and RTO are numbers',
              'HA is surviving a box death. DR is surviving a region death. Back up indexes and registries. Test the restore. A screenshot of a dashboard is not a plan.',
              'The first restore should not be during an incident.',
              'Print rpo and rto.',
              'Disaster recovery requires', 'A tested restore, not only a backup job',
              'print("RPO")\nprint("RTO")', 'RPO\nRTO',
              'print("restore")\nprint("tested")', 'restore\ntested'),
          _s(id, mod, 'ent_08', 'GPU Clusters & Model Serving', 'A fleet, not a hero box',
              'A cluster has a scheduler, a registry, and a serving mesh (vLLM, TGI, Triton). Quotas stop one team from starving another. Observability is per-model.',
              'SSH to the big GPU is not a serving architecture.',
              'Print scheduler and quota.',
              'A serving cluster needs', 'A scheduler, pinned images, and per-model quotas',
              'print("scheduler")\nprint("quota")', 'scheduler\nquota',
              'print("mesh")\nprint("per model")', 'mesh\nper model'),
        ],
      ),
    ],
  );
}
